/**
 * Site-wide font: a single consistent Hebrew-capable font used everywhere
 * (nav, headings, body). Per-destination differentiation is carried by
 * color palette + shape only, not typography — see theme/presets.ts.
 */
export const GOOGLE_FONTS_HREF =
  "https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;700;800&display=swap";

export const SITE_FONT = "'Rubik', sans-serif";

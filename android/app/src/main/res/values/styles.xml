<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Edge-to-edge at the TOP ONLY: transparent status bar so the WebView's
         own page background reaches the physical top of the screen instead
         of a visibly different-colored strip sitting above it. The bottom
         nav bar deliberately stays reserved/opaque (matching the app's own
         background, travi_nav_bar) rather than transparent — content should
         sit above the system nav buttons like any normal app, not draw
         underneath them; see MainActivity's window-insets handling, which
         gives the bottom inset back to the content as padding while leaving
         the top inset alone. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/travi_nav_bar</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>